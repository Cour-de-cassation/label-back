name: opendata

services:
  labelbk:
    build:
      dockerfile: Dockerfile.label.local
    ports:
      - ${LABEL_API_PORT}:${LABEL_API_PORT}
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
